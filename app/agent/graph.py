# TODO(agent-graph-branch): Define the single LangGraph event-consuming agent.
# It must receive transcripts, sensor events, camera-inferred emotion, and MCP
# integration events as one stream, then choose MCP tools via LLM reasoning.
# Do not add hardcoded intent routing here.
