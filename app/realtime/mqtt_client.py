# TODO(realtime-mqtt-branch): Wrap paho-mqtt with reconnect/backoff behavior,
# retained device state, and a bridge that feeds events into the LangGraph agent
# instead of routing commands with intent-specific conditionals.
