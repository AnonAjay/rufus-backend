# TODO(device-service-branch): Implement the backend device service that publishes
# deterministic servo/display commands and consumes sensor acknowledgements over
# the same MQTT topics used by the mock ESP32 and future hardware.
